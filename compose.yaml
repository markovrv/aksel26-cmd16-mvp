services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    restart: unless-stopped
    ports:
      - "15626:15626"
    environment:
      PORT: "15626"
    volumes:
      - app-data:/app/server/db

volumes:
  app-data: