{
  "name": "marshrutka-industrial-career-mvp",
  "version": "2.0.0",
  "private": true,
  "description": "Industrial career guidance quest for students — React + Three.js platform",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "postinstall": "cd client && npm install",
    "test": "cd client && npm run build -- --emptyOutDir && node --check server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}